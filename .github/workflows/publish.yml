name: Publish approved VK posts

on:
  push:
    branches:
      - main
    paths:
      - "publish-request.json"
      - "horoscope-request.json"

permissions:
  contents: read
  statuses: write

concurrency:
  group: vk-publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Download repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install optional story image support
        run: python -m pip install --disable-pip-version-check pillow==11.3.0

      - name: Validate approved requests and publish
        env:
          VK_PUBLISH_TOKEN: ${{ secrets.VK_PUBLISH_TOKEN }}
          VK_USER_TOKEN: ${{ secrets.VK_USER_TOKEN }}
          VK_SERVICE_TOKEN: ${{ secrets.VK_SERVICE_TOKEN }}
          GH_TOKEN: ${{ github.token }}
        run: |
          REQUEST_FILE="publish-request.json"
          if git diff --name-only "${{ github.event.before }}" "${{ github.sha }}" | grep -qx "horoscope-request.json"; then
            REQUEST_FILE="horoscope-request.json"
          fi
          echo "Using request file: $REQUEST_FILE"
          jq -e '.confirmed == true' "$REQUEST_FILE"
          COUNT=$(jq '.posts | length' "$REQUEST_FILE")
          test "$COUNT" -gt 0
          for i in $(seq 0 $((COUNT-1))); do
            if ! jq -e "(.posts[$i].targets // [\"vk\"]) | index(\"vk\") != null" "$REQUEST_FILE" >/dev/null; then
              continue
            fi
            MESSAGE=$(jq -r ".posts[$i].message" "$REQUEST_FILE")
            PUBLISH_DATE=$(jq -r ".posts[$i].publish_date // empty" "$REQUEST_FILE")
            IMAGE_PATH=$(jq -r ".posts[$i].image_path // empty" "$REQUEST_FILE")
            IMAGE_URL=$(jq -r ".posts[$i].image_url // empty" "$REQUEST_FILE")
            SOURCE_POST=$(jq -r ".posts[$i].source_post // empty" "$REQUEST_FILE")
            EDIT_POST_ID=$(jq -r ".posts[$i].edit_post_id // empty" "$REQUEST_FILE")
            STORY_TITLE=$(jq -r ".posts[$i].story_title // empty" "$REQUEST_FILE")
            STORY_TEXT=$(jq -r ".posts[$i].story_text // empty" "$REQUEST_FILE")
            test -n "$MESSAGE"
            ARGS=(--message "$MESSAGE")
            if [ -n "$PUBLISH_DATE" ]; then ARGS+=(--publish-date "$PUBLISH_DATE"); fi
            if [ -n "$EDIT_POST_ID" ]; then ARGS+=(--edit-post-id "$EDIT_POST_ID"); fi
            if [ -n "$IMAGE_URL" ]; then
              [[ "$IMAGE_URL" == https://* ]]
              IMAGE_PATH="$RUNNER_TEMP/vk-image-$i.jpg"
              python -c 'import sys; from urllib.request import Request, urlopen; r=Request(sys.argv[1], headers={"User-Agent":"Mozilla/5.0","Referer":"https://vk.com/"}); open(sys.argv[2],"wb").write(urlopen(r, timeout=120).read())' "$IMAGE_URL" "$IMAGE_PATH"
            elif [ -n "$IMAGE_PATH" ]; then
              [[ "$IMAGE_PATH" == media/* ]]
              test -f "$IMAGE_PATH"
            fi
            if [ -n "$IMAGE_PATH" ]; then ARGS+=(--image "$IMAGE_PATH"); fi
            if [ -n "$SOURCE_POST" ]; then
              [[ "$SOURCE_POST" =~ ^-?[0-9]+_[0-9]+$ ]]
              ARGS+=(--source-post="$SOURCE_POST")
            fi
            if [ -n "$STORY_TITLE" ]; then
              ARGS+=(--duplicate-to-story --story-title "$STORY_TITLE")
              if [ -n "$STORY_TEXT" ]; then ARGS+=(--story-text "$STORY_TEXT"); fi
            fi
            set +e
            OUTPUT=$(CONFIRM_PUBLISH=YES python publisher.py "${ARGS[@]}" 2>&1)
            RESULT=$?
            set -e
            printf '%s\n' "$OUTPUT"
            if [ "$RESULT" -eq 0 ]; then
              POST_ID=$(printf '%s' "$OUTPUT" | python -c 'import json,sys; print(json.load(sys.stdin)["post_id"])')
              STORY_ID=$(printf '%s' "$OUTPUT" | python -c 'import json,sys; data=json.load(sys.stdin); print((data.get("story") or {}).get("story_id", ""))')
              STATE=success
              if [ -n "$STORY_ID" ]; then
                DESCRIPTION="post $POST_ID and story $STORY_ID published"
                CONTEXT="vk-publish/post-$POST_ID-story-$STORY_ID"
              else
                DESCRIPTION="post $POST_ID published"
                CONTEXT="vk-publish/post-$POST_ID"
              fi
            else
              STATE=error
              DESCRIPTION=$(printf '%s' "$OUTPUT" | tail -n 1 | tr -cd '[:alnum:] .:_-' | cut -c1-70)
              CONTEXT="vk-publish/$DESCRIPTION"
            fi
            gh api --method POST "repos/$GITHUB_REPOSITORY/statuses/$GITHUB_SHA" \
              -f state="$STATE" -f context="$CONTEXT" -f description="$DESCRIPTION"
            if [ "$RESULT" -ne 0 ]; then exit "$RESULT"; fi
          done
